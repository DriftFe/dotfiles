background {
    path = /usr/share/backgrounds/archlinux/archlinux-wave.png
    blur_passes = 2
}

input-field {
    monitor = *
    size = 400, 60
    position = 0, -80
    halign = center
    valign = bottom
    placeholder_text = Password:
}

label {
    monitor = *
    text = Hello, World!
    font_size = 32
    font_color = #ffffff
    halign = center
    valign = center
    position = 0, 0
}
